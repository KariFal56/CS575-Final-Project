<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:sys="clr-namespace:System;assembly=mscorlib"
             x:Class="DefectReport.DefectEntryPage">
    <ContentPage.Content>
        
        <StackLayout>
            <Label Text="Defect Information"
                VerticalOptions="CenterAndExpand" 
                HorizontalOptions="CenterAndExpand" />
            <Entry Text="{Binding WorkOrderNumber, Mode=TwoWay}" ReadOnly ="true" />
            <Entry Text="{Binding Count, Mode=TwoWay}" />
            <Entry Text="{Binding Defect, Mode=TwoWay}" />
            <Entry Text="{Binding Disposition, Mode=TwoWay}" />
            <Entry Text="{Binding Date, Mode=TwoWay}" ReadOnly ="true" />
            <Button x:Name="updateButton" Text="Create/Update" Command="{Binding UpdateCommand}" />
            <Button x:Name="deleteButton" Text="Delete" Command="{Binding DeleteCommand}" />
            <Button x:Name="cancelButton" Text="Cancel" Command="{Binding CancelCommand}" />
        </StackLayout>
        
    </ContentPage.Content>
    
</ContentPage>